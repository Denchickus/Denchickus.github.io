{
  "name": "st-technics-prototype",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Локальный прототип нового сайта СТ-ТЕХНИКС. НЕ production. Все формы — mock.",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "node scripts/validate-catalog.mjs && astro build",
    "preview": "astro preview",
    "test": "node --test \"tests/**/*.test.mjs\"",
    "typecheck": "astro check",
    "lint": "astro check",
    "validate:catalog": "node scripts/validate-catalog.mjs",
    "gen:catalog": "node scripts/build-catalog-data.mjs",
    "merge:images": "node scripts/merge-product-images.mjs",
    "check:links": "node scripts/check-links.mjs"
  },
  "dependencies": {
    "astro": "^5.13.7"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.4",
    "typescript": "^5.6.3"
  }
}
